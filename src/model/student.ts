export interface Student {
    discordId: string,
    attendance: string[],
    participations: string[],
    activities: string[],
    activities_grades: string[],
    exams_grades: string,
    participations_grades: string[]
}